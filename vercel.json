{
  "installCommand": "corepack enable && corepack prepare pnpm@10.33.0 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @sonaralabs/web build",
  "outputDirectory": "apps/web/dist",
  "framework": null
}
